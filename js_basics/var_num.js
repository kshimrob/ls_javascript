var numerator = 10;
var denominator = 2;
var answer = numerator / denominator;
var incrementer = 1;
var start = incrementer;
var end;
var difference;

incrementer += 1;
incrementer += 1;
incrementer += 1;

incrementer++;
incrementer++;

end = incrementer;
difference = end - start;

answer = (11 + 31) * 3

console.log(answer);